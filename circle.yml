machine:
  node:
    version: v8.2.0
  environment:
    JVM_OPTS: "-Xms512m -Xmx1024m"
dependencies:
  override:
    - lein deps
    - npm install -g karma-cli
    - ./prepare-tests.sh
test:
  override:
    - ./run-tests.sh
